{
  "name": "deemind",
  "version": "1.0.0",
  "description": "Deemind – Unified Insight Engine CLI",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "deemind": "dist/cli.js"
  },
  "scripts": {
    "dev": "tsup src/cli.ts --watch --format esm,cjs --dts",
    "build": "tsup src/cli.ts --format esm,cjs --dts",
    "start": "node dist/cli.js",
    "lint": "eslint 'src/**/*.{ts,tsx}' --max-warnings=0",
    "test": "vitest"
  },
  "files": ["dist"],
  "keywords": ["sass", "insight", "cli", "nexus", "dependency-graph"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.1.0",
    "chalk": "^5.3.0",
    "zod": "^3.23.8",
    "sass-graph": "^4.0.1"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "tsup": "^8.3.0",
    "eslint": "^9.12.0",
    "vitest": "^1.3.1"
  }
}
