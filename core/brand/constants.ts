import path from 'path';

export const PRESET_METADATA_FILENAME = '.deemind.json';
export const BRAND_PRESET_DIR = path.join(process.cwd(), 'core', 'brands', 'presets');
export const BRAND_FONT_CACHE_DIR = path.join(process.cwd(), '.cache', 'brand-fonts');
