name: AI Docs (stub)
on:
  pull_request:
    types: [opened, synchronize]
jobs:
  draft-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Draft docs summary
        run: |
          echo "# PR Docs Draft" > docs/pr-summary.md
          echo "This is a placeholder generated by CI for PR $GITHUB_REF." >> docs/pr-summary.md
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: pr-docs-draft
          path: docs/pr-summary.md
